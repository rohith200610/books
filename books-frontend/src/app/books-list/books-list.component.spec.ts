import { BooksListComponent } from './books-list.component';
describe('BooksListComponent', () => {
  it('should create', () => {
    const component = new BooksListComponent({} as any);
    expect(component).toBeTruthy();
  });
});
